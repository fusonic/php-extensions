# Upgrade

## 0.3.0
The `DevelopmentFixture` class changed and supports now a `seed` as well as a `locale` which can be set in the constructor. Faker itself will now be instantiated when calling `getFaker`. 
